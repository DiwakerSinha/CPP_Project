#include <iostream>
#include <string>
#include <vector>

#include "WordSearchSolver.hpp"

int main() {
  // std::vector<std::vector<char>> puzzle{{'d', 'e', 'a', 'o', 'u'},
  //                                       {'h', 'e', 'l', 'l', 'o'},
  //                                       {'c', 'a', 'l', 'o', 'm'},
  //                                       {'a', 'e', 't', 'a', 'u'},
  //                                       {'t', 'e', 't', 'o', 'u'}};
  // WordLocation to_return;
  // to_return.word = word;
  // return WordLocation{};
  return 0;
}